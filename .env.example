# Copy to .env and fill in to leave demo mode and sync via Supabase.
# When both are present, the app automatically switches from local-storage
# demo mode to live Supabase mode (see src/supabaseClient.js).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
